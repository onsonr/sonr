syntax = "proto3";

package core;

// Require Go Package
option go_package = ".;models";

// Define Chunk Type: Sent on Data Transfer
message Chunk {
  string b64 = 1;
  bytes buffer = 2;
  int32 size = 3;
  int32 total = 4;
}
