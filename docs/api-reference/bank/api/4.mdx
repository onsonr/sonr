---
title: Fetch Token Info
api: "GET /cosmos/bank/v1beta1/denoms_metadata"
description: DenomsMetadata queries the client metadata of a given coin denomination.
---

<ParamField path="denom" type="string" required>
  denom is the coin denom to query the metadata for.
</ParamField>

<ResponseExample>

```json Response (200)
{
  "metadata": {
    "description": "string",
    "denom_units": [
      {
        "denom": "string",
        "exponent": 0,
        "aliases": ["string"]
      }
    ],
    "base": "string",
    "display": "string",
    "name": "string",
    "symbol": "string",
    "uri": "string",
    "uri_hash": "string"
  }
}
```

```json Error
{
  "code": 0,
  "message": "string",
  "details": [
    {
      "@type": "string"
    }
  ]
}
```

</ResponseExample>
