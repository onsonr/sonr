---
api: POST /cosmos/tx/v1beta1/simulate
title: "Simulate Transaction"
description: "Simulate simulates executing a transaction for estimating gas usage."
---

### Parameters

<ParamField body="tx" type="string" required>
  Tx is the standard type used for broadcasting transactions.
</ParamField>

<ParamField body="tx_bytes" type="string" required>
  tx_bytes is the raw transaction.
</ParamField>

<ResponseExample>

```json Response (200)
{
  "gas_info": {
    "gas_wanted": "string",
    "gas_used": "string"
  },
  "result": {
    "data": "string",
    "log": "string",
    "events": [
      {
        "type": "string",
        "attributes": [
          {
            "key": "string",
            "value": "string",
            "index": true
          }
        ]
      }
    ],
    "msg_responses": [
      {
        "@type": "string"
      }
    ]
  }
}
```

```json Error
{
  "code": 0,
  "message": "string",
  "details": [
    {
      "@type": "string"
    }
  ]
}
```

</ResponseExample>
