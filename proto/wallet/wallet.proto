syntax = "proto3";

package sonr.wallet;
option go_package = "github.com/sonr-io/core/wallet";

// TokenType is the type of keychain.
enum TokenType {
    SNR = 0;
    ETH = 1;
    BTC = 2;
    SOL = 3;
}
