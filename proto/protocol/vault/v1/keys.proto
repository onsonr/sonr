syntax = "proto3";

// Package Motor is used for defining a Motor node and its properties.
package sonrhq.protocol.vault.v1;
option go_package = "github.com/sonr-hq/sonr/core/vault/types/v1";

message PublicKey {
    bytes key = 1;
}

message PrivateKeyShare {
    bytes key = 1;
}
