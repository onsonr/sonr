syntax = "proto3";

package sonrio.sonr.common;

option go_package = "github.com/sonr-io/sonr/x/common/types";

enum LinkKind {
  // None should be specified when no link is provided for the given schema type
  None = 0;
  Object = 1;
  Schema = 2;
  Bucket = 3;
}
