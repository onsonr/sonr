syntax = "proto3";

// Package Motor is used for defining a Motor node and its properties.
package sonrhq.highway.vault;
option go_package = "github.com/sonr-hq/sonr/core/highway/types/vault";

message PublicKey {
    bytes key = 1;
}

message PrivateKeyShare {
    bytes key = 1;
}
