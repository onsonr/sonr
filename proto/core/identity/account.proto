syntax = "proto3";
package core.identity;

option go_package = "github.com/sonrhq/core/x/identity/types";

message ControllerAccount {
  string address = 1;
  string publicKey = 2;
  repeated string authenticators = 3;
  repeated string wallets = 4;
}
