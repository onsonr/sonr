# yaml-language-server: $schema=https://goreleaser.com/static/schema-pro.json
project_name: sonr
before:
  hooks:
    - go mod download

builds:
  # Build - sonrd
  - id: sonrd
    main: cmd/sonrd/main.go
    binary: sonrd
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: linux
        goarch: arm64
    flags:
      - -mod=readonly
      - -trimpath
    ldflags:
      - -X main.Version={{ .Version }}
      - -X main.Commit={{ .Commit }}
      - -w -s
  # Build - hway
  - id: hway
    main: cmd/hway/main.go
    binary: hway
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: linux
        goarch: arm64
    flags:
      - -mod=readonly
      - -trimpath
    ldflags:
      - -X main.Version={{ .Version }}
      - -X main.Commit={{ .Commit }}
      - -w -s
kos:
  # GHCR Docker - sonrd
  - repository: ghcr.io/sonrhq/sonrd
    id: ko-sonrd
    build: sonrd
    main: ./cmd/sonrd
    tags:
      - "{{.Version}}"
      - latest
    bare: true
    preserve_import_paths: true
    platforms:
      - linux/amd64
      - linux/arm64
      - darwin/amd64
      - darwin/arm64
  # GHCR Docker - hway
  - repository: ghcr.io/sonrhq/hway
    id: ko-hway
    build: hway
    main: ./cmd/hway
    tags:
      - "{{.Version}}"
      - latest
    bare: true
    preserve_import_paths: true
    platforms:
      - linux/amd64
      - linux/arm64
      - darwin/amd64
      - darwin/arm64

dockers:
  # Docker Images - sonrd
  - image_templates:
      - "sonrhq/sonrd:{{ .Version }}"
      - sonrhq/sonrd:latest
    build_flag_templates:
      - "--pull"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.title={{.ProjectName}}"
      - "--label=org.opencontainers.image.revision={{.FullCommit}}"
      - "--label=org.opencontainers.image.version={{.Version}}"
    ids:
      - sonrd
  # Docker Images - hway
  - image_templates:
      - "sonrhq/hway:{{ .Version }}"
      - sonrhq/hway:latest
    build_flag_templates:
      - "--pull"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.title={{.ProjectName}}"
      - "--label=org.opencontainers.image.revision={{.FullCommit}}"
      - "--label=org.opencontainers.image.version={{.Version}}"
    ids:
      - hway

archives:
  # Archives
  - id: sonrd
    builds:
      - sonrd
      - hway
    name_template: "{{.Binary}}-{{ .Os }}-{{ .Arch }}"
    format: tar.gz
checksum:
  name_template: sha256sum.txt
  algorithm: sha256

brews:
  # Homebrew - sonrd
  - folder: Formula
    caveats: "Run the Sonr Blockchain Node with `sonrd`"
    homepage: "https://sonr.io"
    description: "Sonr is an ibc-enabled blockchain for decentralized identity."
    ids:
      - "sonrd"
    license: "OpenGLV3"
    name: "sonrd"
    repository:
      owner: sonrhq
      name: homebrew-tap
      token: "{{ .Env.GITHUB_TOKEN }}"
  # Homebrew - hway
  - folder: Formula
    caveats: "Run the Highway Daemon with `hway`"
    homepage: "https://sonr.io"
    description: "Highway Daemon which serves an API Gateway and runs the Sonr Frontend."
    ids:
      - "hway"
    license: "OpenGLV3"
    name: "hway"
    repository:
      owner: sonrhq
      name: homebrew-tap
      token: "{{ .Env.GITHUB_TOKEN }}"
nfpms:
  # linux debs - sonrd
  - id: sonrd
    description: "Sonr is an ibc-enabled blockchain for decentralized identity."
    maintainer: "Sonr Inc."
    homepage: "https://sonr.io"
    license: "OpenGLV3"
    formats:
      - deb
  # linux debs - hway
  - id: hway
    description: "Highway Daemon which serves an API Gateway and runs the Sonr Frontend."
    maintainer: "Sonr Inc."
    homepage: "https://sonr.io"
    license: "OpenGLV3"
    formats:
      - deb

release:
  github:
    owner: sonrhq
    name: sonr

  name_template: "🆔 Release {{ .Version }}"
  mode: replace
  draft: false
  extra_files:
    - glob: ./assets/scripts/install.sh

changelog:
  sort: asc
  abbrev: -1
  groups:
    - title: Features
      regexp: .*?feat:.*$
      order: 0
    - title: Documentation
      regexp: .*?docs:.*$
      order: 1
    - title: Fixes
      regexp: .*?fix:.*$
      order: 2
    - title: Clean Up
      regexp: .*?chore:.*$
      order: 3
    - title: Others
      order: 999

furies:
  - account: sonr
