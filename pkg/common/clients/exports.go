package clients

// QueryClient is the interface which wraps around all the query clients
type QueryClient interface{}

// ServiceClient is the interface which wraps around all the service clients
type ServiceClient interface{}
